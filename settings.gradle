rootProject.name = 'NMS'

file('nms').listFiles()?.each { dir ->
    if (dir.isDirectory()) {
        include ":nms:${dir.name}"
    }
}
include ':common'
include 'nms:api'
include 'common:From_1.21.3'