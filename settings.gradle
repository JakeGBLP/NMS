rootProject.name = 'NMS'

// Include top-level modules
include ':common'
include ':factory'

// Include NMS versions
file('nms').listFiles()?.each { dir ->
    if (dir.isDirectory()) {
        def name = dir.name
        include ":nms:$name"
        project(":nms:$name").projectDir = dir
    }
}
